import type { SegmentFormData } from '@/types/types';

export const SEGMENTS_SOURCE = 'segments-source';
export const DEFAULT_FORM_DATA: SegmentFormData = {
  azimuth: 225,
  deflection: 90,
  distance: 500,
};
